ansible_connection: local

cloudflare_tunnel_token: "{{ vault_cloudflare_tunnel_token }}"
# Matches the live container (confirmed via docker inspect) — cloudflared runs in
# host network mode here, so tunnel-routed services must publish a host port
# (127.0.0.1:<port>) rather than rely on the infra bridge network's container DNS.
cloudflared_network_mode: host

# MQTT
mqtt_username: "{{ vault_mqtt_username }}"
mqtt_password: "{{ vault_mqtt_password }}"

# MSSQL
mssql_sa_password: "{{ vault_mssql_sa_password }}"

# Grafana
grafana_admin_password: "{{ vault_grafana_admin_password }}"
grafana_mssql_database: "S1_Remote_Monitoring"

# Grafana Git Sync — dedicated dashboard repo: https://github.com/Jwagener1/grafana
grafana_git_sync_enabled: true
grafana_git_sync_token: "{{ vault_grafana_git_sync_token }}"
grafana_git_sync_branch: "main"
grafana_git_sync_path: "grafana/"

# Grafana API provisioning — creates users and org on deploy
grafana_api_provision_enabled: true
grafana_orgs:
  - name: "Main Org."

grafana_users:
  - login: "jonathan"
    email: "jonathan@systems-one.com"
    name: "Jonathan"
    password: "{{ vault_grafana_jonathan_password }}"
    orgs:
      - name: "Main Org."
        role: "Admin"
  - login: "avi"
    email: "avi"
    name: "Avi"
    password: "{{ vault_grafana_avi_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"
  - login: "chris"
    email: "chris"
    name: "Chris"
    password: "{{ vault_grafana_chris_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"
  - login: "pkluser"
    email: "pkluser"
    name: "PKL User Group"
    password: "{{ vault_grafana_pkluser_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"
  - login: "cust_bex"
    email: "cust_bex"
    name: "BEX"
    password: "{{ vault_grafana_cust_bex_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"
  - login: "cust_dcb"
    email: "cust_dcb"
    name: "DCB"
    password: "{{ vault_grafana_cust_dcb_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"
  - login: "cust_madibana"
    email: "cust_madibana"
    name: "MADIBANA"
    password: "{{ vault_grafana_cust_madibana_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"
  - login: "cust_pep"
    email: "cust_pep"
    name: "PEP"
    password: "{{ vault_grafana_cust_pep_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"
  - login: "cust_pep_africa"
    email: "cust_pep_africa"
    name: "PEP AFRICA"
    password: "{{ vault_grafana_cust_pep_africa_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"
  - login: "cust_snowsoft"
    email: "cust_snowsoft"
    name: "SNOWSOFT"
    password: "{{ vault_grafana_cust_snowsoft_password }}"
    orgs:
      - name: "Main Org."
        role: "Viewer"

# Grafana MSSQL datasource identity (referenced by dashboards)
grafana_mssql_datasource_uid: "PAAAEB8D7872B0C49"

# s1_reporter chart hosting (Teams notifications) — see Task 9 for Cloudflare DNS setup
s1_reporter_chart_public_base_url: "https://charts.sysone.co.za"

# Offsite backup (Backblaze B2) — see roles/backup. backup_enabled itself is
# a shared default in group_vars/all.yml; flip it here once Step 1's vault
# vars are populated and you've verified {{ backup_script_path }} runs clean.
backup_b2_account_id: "{{ vault_backup_b2_account_id }}"
backup_b2_account_key: "{{ vault_backup_b2_account_key }}"
backup_restic_password: "{{ vault_backup_restic_password }}"
backup_b2_path: "sysone"

# scan_fleet_dashboard's default port (8091) collides with s1_reporter's
# chart-hosting port, which already owns 8091 in production (see
# s1_reporter_chart_public_base_url above). First deploy of this role to
# sysone surfaced the clash -- give it a distinct port instead.
scan_fleet_dashboard_port: 8092

# s1_monitor runs on 8093 until the cutover PR retires marketing_display and frees 8090.
s1_monitor_port: 8093
