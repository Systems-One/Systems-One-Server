name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install linting tools
        run: pip install "ansible-lint~=25.0" "yamllint~=1.35"

      - name: Run yamllint
        run: yamllint -d relaxed .

      - name: Run s1_dashboard unit tests
        run: python roles/s1_dashboard/tests/test_dashboard.py

      - name: Run s1_reporter unit tests
        run: |
          pip install pyyaml
          python -m unittest discover -s roles/s1_reporter/tests

      - name: Run s1_baselines unit tests
        run: |
          pip install pyyaml
          python -m unittest discover -s roles/s1_baselines/tests

      - name: Run systems_one_ingest unit tests
        run: python -m unittest discover -s roles/systems_one_ingest/tests

      - name: Run mqtt_ingestor unit tests
        run: |
          pip install -r roles/mqtt_ingestor/files/app/requirements.txt
          python -m unittest discover -s roles/mqtt_ingestor/tests

      - name: Create placeholder vault password file
        run: echo "ci-placeholder" > .vault_pass

      - name: Run ansible-lint
        id: ansible_lint
        run: ansible-lint --profile=min site.yml
        continue-on-error: true

      - name: Surface ansible-lint result
        if: steps.ansible_lint.outcome == 'failure'
        run: echo "::warning::ansible-lint reported issues — see the 'Run ansible-lint' step log above for details"

  syntax-check:
    name: Syntax Check
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Ansible
        run: pip install "ansible~=14.3"

      - name: Create placeholder vault password file
        run: echo "ci-placeholder" > .vault_pass

      - name: Syntax check site.yml
        run: |
          ansible-playbook site.yml --syntax-check \
            -i production \
            -e "mssql_sa_password=test \
                mssql_app_login_password=test \
                grafana_admin_password=test \
                grafana_jonathan_password=test \
                grafana_avi_password=test \
                grafana_chris_password=test \
                grafana_pkluser_password=test \
                mqtt_password=test \
                grafana_git_sync_token=test \
                cloudflare_tunnel_token=test \
                backup_b2_account_id=test \
                backup_b2_account_key=test \
                backup_restic_password=test"

  molecule:
    name: Molecule (docker role)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install "ansible~=14.3" molecule molecule-plugins[docker] docker

      - name: Run Molecule
        run: molecule test
        env:
          PY_COLORS: "1"
          ANSIBLE_FORCE_COLOR: "1"
          ANSIBLE_ROLES_PATH: "${{ github.workspace }}/roles"
